const AUTO = 'auto';

const ZH = 'zh';

const EN = 'en';

const CONFIG_PATH = '/data/.ezze_config.json';

const BAIDU = 'baidu';

const SCREEN_SHOT_NANE = 'ezze_temp_screenshot.png';

const SCREEN_SELECTED_NAME = 'ezze_temp_selected_screenshot.png';

const ENG_TRAINED_DATA = 'eng.traineddata';

module.exports = {
  AUTO,
  ZH,
  EN,
  CONFIG_PATH,
  BAIDU,
  SCREEN_SHOT_NANE,
  SCREEN_SELECTED_NAME,
  ENG_TRAINED_DATA
}