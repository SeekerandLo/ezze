const AUTO = 'auto';

const ZH = 'zh';

const EN = 'en';

const CONFIG_PATH = '/data/.ezze_config.json';

const BAIDU = 'baidu';

const SCREEN_SHOT_NANE = 'ezze_temp_screenshot.png';

module.exports = {
  AUTO,
  ZH,
  EN,
  CONFIG_PATH,
  BAIDU,
  SCREEN_SHOT_NANE
}